cd() {
  builtin cd "$@" && ls
}
function tm() {
  mate
  if [[ $(ls -l | grep tmproj | wc -l) -eq 0 ]]
  then mate .
  else open *.tmproj
  fi
}
function goaway() {
    local processTerm="$1"
    ps -ef | grep $processTerm | grep -v grep | awk '{print $2}' | xargs kill -9
}